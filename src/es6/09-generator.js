export function* getId() {
    const it =['michi1', 'michi2', 'michi3'];
  
    for (let i = 1; i > 0; i++) {
      for (const letter in arr) {
        yield i + it[michi];
      }
    }
  }
  
  console.log(it.next().value);